* Cline rules are not followed properly.
  - Add the Co-Authored information in a template. Use templates everywhere.
  - Make it clear to never git commit amend or change history.
  - Make rules more concise: NEVER use --no-verify. ALWAYS fix pre-commit hook failures directly.
  - Provide a tool to run bash commands in WSL.
  - Use Rules, Workflows, Skills and Hooks properly.
* Add a way to not dump logs to stdout unless TEST_DEBUG=1 in environment.
* Add Github CI workflow with tests.
* Add proper AI agents tooling.
* Use a U-net model with classification.
* Perform transfer learning on the U-net model.
* Add test case of the --help CLI.
* Use GPU to speed-up.
* Glorot is supposed to be not great with ReLU. Try randn*sqrt(2/n[l-1])
* Use Keras or Torch and continue next steps in recognition using convolutional layers, transfer & multitask learning (C3W2), data augmentation, skip connections, RNNs...
* Find techniques that are efficient for hyper parameters tuning - See C2W3
* Add Recall as a metric - See C3W1
* Package torchvision-alt using numo-narray-alt
* Convert all Ruby methods documentation to a more Ruby standard way
* Extract all ONNX to Torch functionality in a nice Rubygem
